# output/

`agent.py` writes transcripts here. For each file in `input/`, you get one
result file with the same stem:

- `input/chapter1.pdf` -> `output/chapter1.txt` (or `.json` with `--format json`)

Everything in this folder (except this README) is ignored by git.
