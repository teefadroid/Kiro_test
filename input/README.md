# input/

Drop your Arabic PDFs or images here, then run:

```bash
python agent.py
```

Supported formats: `.pdf`, `.png`, `.jpg`, `.jpeg`, `.webp`, `.tif`, `.tiff`, `.bmp`

Everything in this folder (except this README) is ignored by git.
